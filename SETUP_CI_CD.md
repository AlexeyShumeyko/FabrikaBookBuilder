# Инструкция по настройке CI/CD для Fabrika BookBuilder Studio

## ✅ Что уже реализовано

1. **GitHub Actions Workflow** (`.github/workflows/build-and-release.yml`)
   - Автоматическая сборка при push в `main`/`master`
   - Создание установщика в формате ZIP
   - Автоматическое создание релиза на GitHub

2. **Механизм обновлений**
   - Проверка обновлений при запуске приложения
   - UI диалог с информацией об обновлении
   - Автоматическая загрузка и установка обновлений

3. **Установщик**
   - Скрипт создания установщика (`installer/create-installer.ps1`)
   - Автоматическое создание ярлыков
   - Скрипт удаления

## 🔧 Настройка для вашего репозитория

### Шаг 1: Обновите информацию о репозитории

Откройте файл `Services/UpdateService.cs` и замените:

```csharp
private const string Owner = "ВашGitHubUsername"; // Замените на ваш GitHub username
private const string Repo = "PhotoBookRenamer"; // Замените на название вашего репозитория
```

### Шаг 2: Проверьте workflow файл

Откройте `.github/workflows/build-and-release.yml` и убедитесь, что:
- Названия веток правильные (`main` или `master`)
- Имя проекта соответствует (`PhotoBookRenamer`)

### Шаг 3: Настройте версионирование

Версия указывается в `PhotoBookRenamer.csproj`:
```xml
<Version>1.0.0</Version>
```

**Важно:** При изменении версии автоматически создается новый релиз с тегом `v1.0.0`.

## 📦 Процесс работы

### Для разработчика:

1. Внесите изменения в код
2. Обновите версию в `PhotoBookRenamer.csproj` (если необходимо)
3. Сделайте commit и push:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

4. GitHub Actions автоматически:
   - Соберет проект
   - Создаст установщик
   - Создаст релиз на GitHub

### Для пользователя:

1. При открытии приложения автоматически проверяется наличие обновлений
2. Если обновление доступно, показывается диалог с:
   - Текущей и новой версией
   - Release notes (что нового)
   - Кнопками "Обновить" и "Отложить"
3. При нажатии "Обновить":
   - Загружается установщик
   - Показывается прогресс загрузки
   - Запускается установка (требуются права администратора)
   - Приложение закрывается для установки

## 🚀 Тестирование

### Локальная сборка установщика:

```powershell
# 1. Опубликуйте приложение
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true

# 2. Создайте установщик
.\installer\create-installer.ps1 -Version "1.0.0" -PublishPath ".\bin\Release\net8.0-windows\win-x64\publish"
```

### Тестирование обновлений:

1. Создайте тестовый релиз на GitHub:
   - Перейдите в раздел Releases
   - Нажмите "Draft a new release"
   - Укажите тег версии (например, `v1.0.1`)
   - Загрузите ZIP файл с установщиком
   - Опубликуйте релиз

2. Запустите приложение с более старой версией
3. Проверьте, что диалог обновления появляется

## ⚠️ Важные замечания

1. **Права администратора:** Установка обновлений требует прав администратора. Пользователь должен запустить приложение от имени администратора или подтвердить UAC запрос.

2. **GitHub Token:** GitHub Actions автоматически использует `GITHUB_TOKEN` для создания релизов. Дополнительная настройка не требуется.

3. **Версионирование:** Используйте семантическое версионирование (SemVer):
   - `MAJOR.MINOR.PATCH` (например, `1.0.0`)
   - При критических изменениях увеличивайте MAJOR
   - При новых функциях увеличивайте MINOR
   - При исправлениях увеличивайте PATCH

4. **Release Notes:** GitHub Actions автоматически создает базовые release notes. Вы можете редактировать их после создания релиза.

## 🔍 Troubleshooting

### Обновления не обнаруживаются:
- Проверьте `Owner` и `Repo` в `UpdateService.cs`
- Убедитесь, что релиз создан на GitHub
- Проверьте, что тег релиза начинается с `v` (например, `v1.0.0`)

### Ошибки при сборке:
- Убедитесь, что все зависимости установлены
- Проверьте версию .NET SDK (8.0.x)

### Ошибки при установке:
- Убедитесь, что приложение запущено от имени администратора
- Проверьте права доступа к папке установки (`C:\Program Files\Fabrika BookBuilder Studio`)

## 📝 Дополнительная информация

Подробная документация находится в файле `README_CI_CD.md`.

