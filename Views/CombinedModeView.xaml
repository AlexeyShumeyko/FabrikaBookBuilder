<UserControl x:Class="PhotoBookRenamer.Views.CombinedModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:utils="clr-namespace:PhotoBookRenamer.Utils">
    <Grid Background="#eceff5" PreviewMouseDown="OnGridPreviewMouseDown" ClipToBounds="True"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/> <!-- Верхняя панель -->
            <RowDefinition Height="*"/> <!-- Рабочая область -->
        </Grid.RowDefinitions>
        
        <!-- Верхняя панель (в стиле HTML) -->
        <Border Grid.Row="0" 
                Background="White"
                BorderBrush="#E7E0EC"
                BorderThickness="0,0,0,1"
                Height="64">
            <Border.Effect>
                <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
            </Border.Effect>
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="200"/>
                    <ColumnDefinition Width="*" MinWidth="400"/>
                    <ColumnDefinition Width="*" MinWidth="250"/>
                </Grid.ColumnDefinitions>
                
                <!-- 1. Информация о проекте -->
                <StackPanel Grid.Column="0" 
                            Orientation="Vertical" 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left">
                    <Grid>
                        <!-- TextBlock для отображения (по умолчанию видим) -->
                        <TextBlock x:Name="ProjectNameTextBlock"
                                   Text="{Binding ProjectName, FallbackValue='Новый проект 2025-12-02 15:11'}" 
                                   FontSize="20"
                                   FontWeight="600"
                                   Foreground="#1C1B1F"
                                   UseLayoutRounding="True"
                                   SnapsToDevicePixels="True"
                                   TextOptions.TextFormattingMode="Display"
                                   TextOptions.TextRenderingMode="ClearType"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   MouseLeftButtonDown="OnProjectNameClick"
                                   Cursor="Hand"
                                   Padding="2,0"/>
                        <!-- TextBox для редактирования (по умолчанию скрыт) -->
                        <TextBox x:Name="ProjectNameTextBox"
                                 FontSize="20"
                                 FontWeight="600"
                                 Foreground="#1C1B1F"
                                 Background="White"
                                 BorderBrush="#6750A4"
                                 BorderThickness="2"
                                 Padding="4,2"
                                 VerticalContentAlignment="Center"
                                 LostFocus="OnProjectNameLostFocus"
                                 KeyDown="OnProjectNameKeyDown"
                                 Visibility="Collapsed"
                                 MinWidth="200"/>
                    </Grid>
                    <TextBlock Text="Комбинированный режим" 
                               FontSize="14"
                               Foreground="#625B71"
                               Margin="0,4,0,0"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                </StackPanel>
                
                <!-- 2. Конфигурация книг (компактная, не скрывается) -->
                <Grid Grid.Column="1" 
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center">
                    <Border Background="#FEF7FF"
                            BorderBrush="#E7E0EC"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="12,8"
                            MinWidth="400"
                            MaxWidth="500">
                        <StackPanel Orientation="Horizontal" 
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,0,12,0" VerticalAlignment="Center">
                                <TextBlock Text="Книг:" 
                                           FontSize="13"
                                           Foreground="#625B71"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                <Border BorderBrush="#79747E"
                                        BorderThickness="1"
                                        Background="White"
                                        CornerRadius="8"
                                        Width="50"
                                        Height="32"
                                        VerticalAlignment="Center">
                                    <TextBox Text="{Binding NumberOfBooks, UpdateSourceTrigger=LostFocus}" 
                                             Padding="4,0"
                                             PreviewTextInput="NumberValidationTextBox"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             VerticalContentAlignment="Center"
                                             HorizontalContentAlignment="Center"
                                             FontSize="13"
                                             VerticalAlignment="Stretch"
                                             HorizontalAlignment="Stretch"
                                             Focusable="True"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,12,0" VerticalAlignment="Center">
                                <TextBlock Text="Разворотов:" 
                                           FontSize="13"
                                           Foreground="#625B71"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                <Border BorderBrush="#79747E"
                                        BorderThickness="1"
                                        Background="White"
                                        CornerRadius="8"
                                        Width="50"
                                        Height="32"
                                        VerticalAlignment="Center">
                                    <TextBox Text="{Binding SpreadsPerBook, UpdateSourceTrigger=LostFocus}" 
                                             Padding="4,0"
                                             PreviewTextInput="NumberValidationTextBox"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             VerticalContentAlignment="Center"
                                             HorizontalContentAlignment="Center"
                                             FontSize="13"
                                             VerticalAlignment="Stretch"
                                             HorizontalAlignment="Stretch"
                                             Focusable="True"/>
                                </Border>
                            </StackPanel>
                            <Button Command="{Binding ConfirmStructureCommand}"
                                    Padding="10,6"
                                    Height="32"
                                    FontSize="13"
                                    FontWeight="500"
                                    Cursor="Hand"
                                    VerticalAlignment="Center"
                                    MinWidth="90">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#6750A4"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <Border.Effect>
                                                            <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                                        </Border.Effect>
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5A4B8F"/>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <TranslateTransform Y="-1"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="+" FontSize="14" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Создать" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
                
                <!-- 3. Кнопки действий (в стиле HTML) -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right">
                    <Button Command="{Binding BackCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FEF7FF"/>
                                <Setter Property="Foreground" Value="#6750A4"/>
                                <Setter Property="BorderBrush" Value="#79747E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E7E0EC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="←" FontSize="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Назад" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding SaveProjectCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FEF7FF"/>
                                <Setter Property="Foreground" Value="#6750A4"/>
                                <Setter Property="BorderBrush" Value="#79747E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E7E0EC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" FontSize="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Сохранить" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#6750A4"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5A4B8F"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <TranslateTransform Y="-1"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📤" FontSize="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Экспорт" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenHelpCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,0,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#625B71"/>
                                <Setter Property="BorderBrush" Value="#79747E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F3FF"/>
                                        <Setter Property="Foreground" Value="#6750A4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="?" FontSize="18" FontWeight="600" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Помощь" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Рабочая область -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" MinWidth="280" MaxWidth="400"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>
            
            <!-- Левая панель - Загруженные файлы (в стиле HTML) -->
            <Border Grid.Column="0" 
                    Background="White"
                    BorderBrush="{StaticResource SurfaceVariant}"
                    BorderThickness="1"
                    CornerRadius="20"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    ClipToBounds="False">
                <Border.Effect>
                    <StaticResource ResourceKey="Shadow2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto" MinHeight="200"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Заголовок панели (в стиле HTML) -->
                    <Border Grid.Row="0" 
                            Background="#EADDFF"
                            CornerRadius="20,20,0,0"
                            Padding="16,16">
                        <Grid>
                            <TextBlock Text="Загруженные файлы" 
                                       FontSize="16"
                                       Foreground="#6750A4"
                                       FontWeight="600"
                                       VerticalAlignment="Center"/>
                            <Button Command="{Binding ClearFilesCommand}"
                                    Padding="4,4"
                                    FontSize="12"
                                    Height="Auto"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#FEF7FF"/>
                                        <Setter Property="Foreground" Value="#6750A4"/>
                                        <Setter Property="BorderBrush" Value="#79747E"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E7E0EC"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🗑️" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Очистить всё" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                    
                    <!-- Область загрузки (в стиле HTML) -->
                    <Border Grid.Row="1" 
                            Background="#FEF7FF"
                            BorderBrush="#E7E0EC"
                            BorderThickness="2"
                            CornerRadius="12"
                            Margin="16,8"
                            Padding="20,8"
                            Cursor="Hand"
                            MouseLeftButtonDown="OnUploadAreaClick"
                            Loaded="OnUploadAreaLoaded"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            MinHeight="110"
                            MaxHeight="140"
                            ClipToBounds="True">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#6750A4"/>
                                        <Setter Property="Background" Value="#EADDFF"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <TranslateTransform Y="-2"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel Orientation="Vertical" 
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <TextBlock Text="☁️" 
                                       FontSize="40" 
                                       Foreground="#79747E"
                                       HorizontalAlignment="Center"
                                       Margin="0,-2,0,2"/>
                            <TextBlock Text="Перетащите файлы сюда" 
                                       FontWeight="500"
                                       FontSize="16"
                                       Foreground="#1C1B1F"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="или нажмите для выбора" 
                                       FontSize="15"
                                       Foreground="#625B71"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,6"/>
                            <TextBlock Text="JPG, JPEG до 50 МБ" 
                                       FontSize="13"
                                       Foreground="#79747E"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Сетка файлов (в стиле HTML - 3 колонки) -->
                    <ScrollViewer Grid.Row="2" 
                                  VerticalScrollBarVisibility="Auto" 
                                  Padding="16">
                        <ItemsControl ItemsSource="{Binding AvailableFiles}"
                                      AllowDrop="True"
                                      Drop="OnFileListDrop">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource Surface}"
                                            BorderBrush="Transparent"
                                            BorderThickness="2"
                                            CornerRadius="8"
                                            Margin="4"
                                            Cursor="Hand"
                                            AllowDrop="True"
                                            PreviewMouseLeftButtonDown="OnFileItemMouseDown">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                                        <Setter Property="RenderTransform">
                                                            <Setter.Value>
                                                                <TranslateTransform Y="-2"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <!-- Превью изображения (70% высоты, aspect-ratio: 1) -->
                                            <Border Grid.Row="0" 
                                                    Background="{StaticResource SurfaceVariant}"
                                                    CornerRadius="6,6,0,0">
                                                <Border.Height>
                                                    <Binding RelativeSource="{RelativeSource AncestorType=Border}" Path="ActualWidth"/>
                                                </Border.Height>
                                                <Image Stretch="UniformToFill"
                                                       Source="{Binding Converter={StaticResource FilePathToThumbnailConverter}, Path=.}"
                                                       HorizontalAlignment="Stretch"
                                                       VerticalAlignment="Stretch"/>
                                            </Border>
                                            
                                            <!-- Имя файла (в стиле HTML) -->
                                            <TextBlock Grid.Row="1" 
                                                       Text="{Binding Converter={StaticResource FileNameConverter}, Path=.}" 
                                                       Style="{StaticResource BodySmall}"
                                                       FontSize="11"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextAlignment="Center"
                                                       Padding="4"
                                                       Foreground="{StaticResource TextPrimary}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Разделитель панелей -->
            <GridSplitter Grid.Column="1"
                         Width="8"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Background="Transparent"
                         Cursor="SizeWE"
                         ShowsPreview="False"
                         ResizeDirection="Columns"/>
            
            <!-- Правая панель - Редактирование книг (в стиле HTML) -->
            <Border Grid.Column="2" 
                    Background="White"
                    BorderBrush="{StaticResource SurfaceVariant}"
                    BorderThickness="1"
                    CornerRadius="20"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    ClipToBounds="False">
                <Border.Effect>
                    <StaticResource ResourceKey="Shadow2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Заголовок панели (в стиле HTML) -->
                    <Border Grid.Row="0" 
                            Background="#EADDFF"
                            CornerRadius="20,20,0,0"
                            Padding="16,16">
                        <Grid>
                            <TextBlock Text="Редактирование книг" 
                                       FontSize="16"
                                       Foreground="#6750A4"
                                       FontWeight="600"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="Перетащите фото в ячейки" 
                                       FontSize="14"
                                       Foreground="#625B71"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                    
                    <!-- Контейнер книг (в стиле HTML) -->
                    <ScrollViewer Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto" 
                                  Padding="24">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding Books}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FEF7FF"
                                                BorderBrush="#E7E0EC"
                                                BorderThickness="1"
                                                CornerRadius="20"
                                                Padding="24"
                                                Margin="0,0,0,32">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                            </Border.Effect>
                                            <StackPanel>
                                                <!-- Заголовок книги (в стиле HTML) -->
                                                <Grid Margin="0,0,0,24">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <!-- Разделитель -->
                                                    <Border Grid.Row="1"
                                                            BorderThickness="0,0,0,1"
                                                            BorderBrush="#E7E0EC"
                                                            Margin="0,16,0,0"/>
                                                    
                                                    <!-- Контент заголовка -->
                                                    <Grid Grid.Row="0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" 
                                                                               FontSize="18"
                                                                               Foreground="#6750A4"
                                                                               FontWeight="600"/>
                                                                    <!-- Индикатор статуса книги (после названия) - только цветной кружок -->
                                                                    <Ellipse Width="16"
                                                                             Height="16"
                                                                             Margin="8,0,0,0"
                                                                             VerticalAlignment="Center">
                                                                        <Ellipse.Style>
                                                                            <Style TargetType="Ellipse">
                                                                                <Setter Property="Fill" Value="#FF5252"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding Path=IsValid}" Value="True">
                                                                                        <Setter Property="Fill" Value="#4CAF50"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Ellipse.Style>
                                                                    </Ellipse>
                                                                </StackPanel>
                                                                <TextBlock FontSize="14"
                                                                           Foreground="#625B71"
                                                                           Margin="0,4,0,0">
                                                                    <TextBlock.Text>
                                                                        <Binding Path="Pages.Count" StringFormat="{}{0} разворотов"/>
                                                                    </TextBlock.Text>
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </StackPanel>
                                                        
                                                        <!-- Кнопка "Дублировать книгу" -->
                                                        <Button Grid.Column="1" 
                                                                Command="{Binding DataContext.DuplicateBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Padding="12,6"
                                                                FontSize="12"
                                                                Height="Auto"
                                                                Cursor="Hand"
                                                                VerticalAlignment="Center"
                                                                Margin="0,0,8,0">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="#FEF7FF"/>
                                                                    <Setter Property="Foreground" Value="#6750A4"/>
                                                                    <Setter Property="BorderBrush" Value="#79747E"/>
                                                                    <Setter Property="BorderThickness" Value="1"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                                        CornerRadius="8"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#E7E0EC"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                                <TextBlock Text="Дублировать" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                        
                                                        <!-- Кнопка "Удалить" -->
                                                        <Button Grid.Column="2" 
                                                                Command="{Binding DataContext.DeleteBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Padding="12,6"
                                                                FontSize="12"
                                                                Height="Auto"
                                                                Cursor="Hand"
                                                                VerticalAlignment="Center">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="#FEF7FF"/>
                                                                    <Setter Property="Foreground" Value="#6750A4"/>
                                                                    <Setter Property="BorderBrush" Value="#79747E"/>
                                                                    <Setter Property="BorderThickness" Value="1"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                                        CornerRadius="8"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#E7E0EC"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="🗑️" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                                <TextBlock Text="Удалить" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </Grid>
                                                </Grid>
                                                
                                                <!-- Сетка слотов (4 в ряд, на всю ширину) -->
                                                <ItemsControl ItemsSource="{Binding AllSlotsPages}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <UniformGrid Columns="4"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                                    <Border CornerRadius="12"
                                                                            Padding="8"
                                                                            Margin="6"
                                                                            AllowDrop="True"
                                                                            Drop="OnSlotDrop"
                                                                            DragOver="OnSlotDragOver"
                                                                            MouseLeftButtonDown="OnSlotClick"
                                                                            Tag="{Binding}"
                                                                            DataContext="{Binding}"
                                                                            Height="220"
                                                                            Loaded="OnCellLoaded">
                                                                        <Border.Style>
                                                                            <Style TargetType="Border">
                                                                                <Setter Property="BorderBrush" Value="#E7E0EC"/>
                                                                                <Setter Property="BorderThickness" Value="2"/>
                                                                                <Setter Property="Background" Value="White"/>
                                                                                <Style.Triggers>
                                                                                    <!-- КРИТИЧЕСКИ ВАЖНО: Светло-нежный зелёный фон для заполненных ячеек (БЕЗ наведения) -->
                                                                                    <DataTrigger Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="True">
                                                                                        <Setter Property="Background" Value="#F1F8F4"/>
                                                                                        <Setter Property="BorderBrush" Value="#C8E6D5"/>
                                                                                        <Setter Property="Cursor" Value="Arrow"/>
                                                                                    </DataTrigger>
                                                                                    <!-- КРИТИЧЕСКИ ВАЖНО: Курсор для пустых ячеек -->
                                                                                    <DataTrigger Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="False">
                                                                                        <Setter Property="Cursor" Value="Hand"/>
                                                                                    </DataTrigger>
                                                                                    <!-- При наведении на заполненную ячейку - немного более насыщенный зелёный (ПЕРВЫМ, чтобы переопределить базовый цвет) -->
                                                                                    <MultiDataTrigger>
                                                                                        <MultiDataTrigger.Conditions>
                                                                                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                                                                                            <Condition Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="True"/>
                                                                                        </MultiDataTrigger.Conditions>
                                                                                        <Setter Property="Background" Value="#E8F5ED"/>
                                                                                        <Setter Property="BorderBrush" Value="#B8E6C8"/>
                                                                                        <Setter Property="RenderTransform">
                                                                                            <Setter.Value>
                                                                                                <TranslateTransform Y="-2"/>
                                                                                            </Setter.Value>
                                                                                        </Setter>
                                                                                        <Setter Property="Effect">
                                                                                            <Setter.Value>
                                                                                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                                                                            </Setter.Value>
                                                                                        </Setter>
                                                                                    </MultiDataTrigger>
                                                                                    <!-- При наведении на пустую ячейку -->
                                                                                    <MultiDataTrigger>
                                                                                        <MultiDataTrigger.Conditions>
                                                                                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                                                                                            <Condition Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="False"/>
                                                                                        </MultiDataTrigger.Conditions>
                                                                                        <Setter Property="BorderBrush" Value="#6750A4"/>
                                                                                        <Setter Property="Background" Value="#EADDFF"/>
                                                                                        <Setter Property="RenderTransform">
                                                                                            <Setter.Value>
                                                                                                <TranslateTransform Y="-2"/>
                                                                                            </Setter.Value>
                                                                                        </Setter>
                                                                                        <Setter Property="Effect">
                                                                                            <Setter.Value>
                                                                                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                                                                            </Setter.Value>
                                                                                        </Setter>
                                                                                    </MultiDataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Border.Style>
                                                                <Grid>
                                                                    <!-- Номер страницы (в кружке, top-left) -->
                                                                    <Grid HorizontalAlignment="Left"
                                                                          VerticalAlignment="Top"
                                                                          Width="24"
                                                                          Height="24"
                                                                          Margin="0,0,0,0"
                                                                          Panel.ZIndex="10">
                                                                        <Grid.Style>
                                                                            <Style TargetType="Grid">
                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding Converter={StaticResource SlotIndexToPageNumberConverter}}" Value="">
                                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Grid.Style>
                                                                        <Ellipse Fill="#6750A4"/>
                                                                        <TextBlock Text="{Binding Converter={StaticResource SlotIndexToPageNumberConverter}}"
                                                                                   Foreground="White"
                                                                                   FontSize="12"
                                                                                   FontWeight="600"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"/>
                                                                    </Grid>
                                                                    
                                                                    <!-- Кнопки действий (bottom-center, только когда есть фото) -->
                                                                    <StackPanel HorizontalAlignment="Center"
                                                                                VerticalAlignment="Bottom"
                                                                                Orientation="Horizontal"
                                                                                Margin="0,0,0,0"
                                                                                Panel.ZIndex="20">
                                                                        <StackPanel.Style>
                                                                            <Style TargetType="StackPanel">
                                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                                <Style.Triggers>
                                                                                    <!-- Показываем кнопки только когда есть фото -->
                                                                                    <DataTrigger Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="True">
                                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </StackPanel.Style>
                                                                        
                                                                        <!-- Кнопка "Дублировать во все книги" -->
                                                                        <Button Command="{Binding DataContext.DuplicateToAllBooksCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                CommandParameter="{Binding}"
                                                                                Width="36"
                                                                                Height="36"
                                                                                Padding="0"
                                                                                Margin="0,0,6,0"
                                                                                Cursor="Hand"
                                                                                ToolTip="Дублировать во все книги">
                                                                            <Button.Style>
                                                                                <Style TargetType="Button">
                                                                                    <Setter Property="Background" Value="#6750A4"/>
                                                                                    <Setter Property="Foreground" Value="White"/>
                                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                                    <Setter Property="Template">
                                                                                        <Setter.Value>
                                                                                            <ControlTemplate TargetType="Button">
                                                                                                <Border Background="{TemplateBinding Background}"
                                                                                                        CornerRadius="8"
                                                                                                        Padding="8">
                                                                                                    <Border.Effect>
                                                                                                        <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
                                                                                                    </Border.Effect>
                                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                                </Border>
                                                                                            </ControlTemplate>
                                                                                        </Setter.Value>
                                                                                    </Setter>
                                                                                    <Style.Triggers>
                                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                                            <Setter Property="Background" Value="#8651F3"/>
                                                                                            <Setter Property="RenderTransform">
                                                                                                <Setter.Value>
                                                                                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                                                                                </Setter.Value>
                                                                                            </Setter>
                                                                                        </Trigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </Button.Style>
                                                                            <TextBlock Text="📋" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Button>
                                                                        
                                                                        <!-- Кнопка "Удалить" -->
                                                                        <Button Command="{Binding DataContext.DeletePageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                CommandParameter="{Binding}"
                                                                                Width="36"
                                                                                Height="36"
                                                                                Padding="0"
                                                                                Cursor="Hand"
                                                                                ToolTip="Удалить фото">
                                                                            <Button.Style>
                                                                                <Style TargetType="Button">
                                                                                    <Setter Property="Background" Value="#FF5252"/>
                                                                                    <Setter Property="Foreground" Value="White"/>
                                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                                    <Setter Property="Template">
                                                                                        <Setter.Value>
                                                                                            <ControlTemplate TargetType="Button">
                                                                                                <Border Background="{TemplateBinding Background}"
                                                                                                        CornerRadius="8"
                                                                                                        Padding="8">
                                                                                                    <Border.Effect>
                                                                                                        <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
                                                                                                    </Border.Effect>
                                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                                </Border>
                                                                                            </ControlTemplate>
                                                                                        </Setter.Value>
                                                                                    </Setter>
                                                                                    <Style.Triggers>
                                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                                            <Setter Property="Background" Value="#E53935"/>
                                                                                            <Setter Property="RenderTransform">
                                                                                                <Setter.Value>
                                                                                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                                                                                </Setter.Value>
                                                                                            </Setter>
                                                                                        </Trigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </Button.Style>
                                                                            <TextBlock Text="🗑️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Button>
                                                                    </StackPanel>
                                                                    
                                                                    <!-- Контент слота - Grid структура как в UniqueFoldersView -->
                                                                    <Grid>
                                                                        <Grid.RowDefinitions>
                                                                            <RowDefinition Height="Auto"/>
                                                                            <RowDefinition Height="*"/>
                                                                        </Grid.RowDefinitions>
                                                                        
                                                                        <!-- Плейсхолдер иконка (показывается когда НЕТ изображения) -->
                                                                        <TextBlock Grid.Row="0"
                                                                                   Grid.RowSpan="2"
                                                                                   Text="🏔️" 
                                                                                   FontSize="48" 
                                                                                   Foreground="#79747E"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"
                                                                                   Panel.ZIndex="1">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="False">
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                        
                                                                        <!-- Подпись (показывается только когда НЕТ изображения) -->
                                                                        <TextBlock Grid.Row="1"
                                                                                   Text="Добавить фото" 
                                                                                   FontSize="12"
                                                                                   Foreground="#625B71"
                                                                                   FontWeight="500"
                                                                                   TextAlignment="Center"
                                                                                   VerticalAlignment="Bottom"
                                                                                   Margin="0,0,0,8"
                                                                                   Panel.ZIndex="2">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="False">
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                        
                                                                        <!-- Фото - Grid растягивается по ширине ячейки -->
                                                                        <Grid Grid.Row="1"
                                                                              Background="Transparent"
                                                                              ClipToBounds="True"
                                                                              HorizontalAlignment="Stretch"
                                                                              VerticalAlignment="Stretch"
                                                                              Loaded="OnPhotoGridLoaded">
                                                                            <Image Stretch="Fill"
                                                                                   HorizontalAlignment="Stretch"
                                                                                   VerticalAlignment="Stretch"
                                                                                   Cursor="Hand"
                                                                                   ToolTip="Двойной клик для открытия"
                                                                                   Loaded="OnImageLoaded"
                                                                                   SizeChanged="OnImageSizeChanged"
                                                                                   LayoutUpdated="OnImageLayoutUpdated"
                                                                                   MouseLeftButtonDown="OnImageDoubleClick">
                                                                                <Image.Source>
                                                                                    <Binding Converter="{StaticResource PageSourceConverter}"/>
                                                                                </Image.Source>
                                                                                <Image.Style>
                                                                                    <Style TargetType="Image">
                                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                                        <Style.Triggers>
                                                                                            <DataTrigger Binding="{Binding Path=SourcePath, Converter={StaticResource StringToBooleanConverter}}" Value="True">
                                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                                            </DataTrigger>
                                                                                        </Style.Triggers>
                                                                                    </Style>
                                                                                </Image.Style>
                                                                                <Image.Tag>
                                                                                    <Binding/>
                                                                                </Image.Tag>
                                                                            </Image>
                                                                        </Grid>
                                                                    </Grid>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Индикатор загрузки -->
        <Grid Grid.RowSpan="2" 
              Background="#CC000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource ModernElevatedCard}"
                    Padding="48,40" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    MinWidth="360">
                <StackPanel>
                    <TextBlock Text="Загрузка..." 
                               Style="{StaticResource TitleMedium}"
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,32"/>
                    <ProgressBar IsIndeterminate="True" 
                                 Height="6" 
                                 Width="300"
                                 HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
