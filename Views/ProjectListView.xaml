<UserControl x:Class="PhotoBookRenamer.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PhotoBookRenamer.ViewModels"
             xmlns:models="clr-namespace:PhotoBookRenamer.Models"
             xmlns:utils="clr-namespace:PhotoBookRenamer.Utils"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             RenderOptions.BitmapScalingMode="HighQuality"
             RenderOptions.ClearTypeHint="Enabled">
    <Grid Background="#eceff5" 
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          ClipToBounds="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="68"/> <!-- Верхняя панель -->
            <RowDefinition Height="*"/> <!-- Основной контент -->
        </Grid.RowDefinitions>
        
        <!-- Верхняя панель (в стиле HTML) -->
        <Border Grid.Row="0" 
                Background="White"
                BorderBrush="{StaticResource Outline}"
                BorderThickness="0,0,0,1"
                Height="68">
            <Border.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="0" Opacity="0.08" Color="#000000" Direction="270"/>
            </Border.Effect>
            <Grid Margin="32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Заголовок страницы -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Мои проекты" 
                               FontSize="22"
                               FontWeight="700"
                               Foreground="{StaticResource TextPrimary}"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="Управляйте созданными фотокнигами и создавайте новые" 
                               FontSize="14"
                               Foreground="{StaticResource TextSecondary}"
                               Margin="0,4,0,0"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                </StackPanel>
                
                <!-- Кнопка помощи -->
                <Button Grid.Column="1"
                        Command="{Binding OpenHelpCommand}"
                        Padding="16,10"
                        Height="40"
                        FontSize="14"
                        FontWeight="500"
                        Margin="0,0,12,0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#625B71"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="#79747E"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F3FF"/>
                                    <Setter Property="Foreground" Value="#6750A4"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="?" FontSize="18" FontWeight="600" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Помощь" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- Кнопка создания проекта -->
                <Button Grid.Column="2" 
                        Command="{Binding CreateProjectCommand}"
                        Padding="18,10"
                        Height="40"
                        FontSize="14"
                        FontWeight="600"
                        UseLayoutRounding="True"
                        SnapsToDevicePixels="True"
                        TextOptions.TextFormattingMode="Display"
                        TextOptions.TextRenderingMode="ClearType"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#6750a4"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}"
                                                SnapsToDevicePixels="True">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#8651f3"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="+" 
                                   FontSize="18" 
                                   FontWeight="700" 
                                   Margin="0,-4,8,0" 
                                   VerticalAlignment="Center"
                                   LineHeight="18"/>
                        <TextBlock Text="Создать проект" 
                                   VerticalAlignment="Center"
                                   FontSize="14"
                                   FontWeight="600"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
        
        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="16,24">
            <StackPanel>
                <!-- Пустое состояние -->
                <StackPanel Visibility="{Binding Projects.Count, Converter={StaticResource IsZeroToVisibilityConverter}}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="0,80">
                    <Border Background="White"
                            BorderBrush="{StaticResource Outline}"
                            BorderThickness="2"
                            CornerRadius="20"
                            Padding="64,48" 
                            MaxWidth="800">
                        <StackPanel>
                            <Image x:Name="AppLogoImage"
                                   Width="120" 
                                   Height="120"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,32"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   Loaded="AppLogoImage_Loaded"/>
                            
                            <TextBlock Text="Добро пожаловать в Fabrika BookBuilder Studio" 
                                       FontSize="24"
                                       FontWeight="700"
                                       Foreground="{StaticResource TextPrimary}"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,20"/>
                            
                            <TextBlock Text="Профессиональный инструмент для подготовки фотокниг к печати." 
                                       FontSize="16"
                                       FontWeight="500"
                                       Foreground="{StaticResource TextPrimary}"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,20"
                                       TextAlignment="Center"/>
                            
                            <TextBlock TextWrapping="Wrap" 
                                       FontSize="15"
                                       Foreground="{StaticResource TextSecondary}"
                                       TextAlignment="Center"
                                       LineHeight="26"
                                       Margin="0,0,0,32">
                                <Run Text="Создавайте и настраивайте фотокниги быстро: пакетная обработка папок, гибкое распределение разворотов и удобный drag &amp; drop-интерфейс."/>
                            </TextBlock>
                            
                            <Button Command="{Binding CreateProjectCommand}"
                                    HorizontalAlignment="Center"
                                    Padding="24,12"
                                    FontSize="14"
                                    FontWeight="500"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <Border.Effect>
                                                            <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                                        </Border.Effect>
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5A4B8F"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="+" FontSize="18" FontWeight="700" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Создать проект" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
                
                <!-- Список проектов (вертикальный список, как в HTML) -->
                <ItemsControl ItemsSource="{Binding Projects}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ProjectInfo}">
                            <Border Background="#ffffff"
                                    BorderBrush="{StaticResource Outline}"
                                    BorderThickness="1"
                                    CornerRadius="20"
                                    Padding="24"
                                    Margin="0,0,0,16"
                                    ClipToBounds="False"
                                    Effect="{StaticResource ShadowCard}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryLight}"/>
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <TranslateTransform Y="-2"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Значок режима (слева) -->
                                    <Border Grid.Column="0"
                                            Width="48"
                                            Height="48"
                                            Background="#eaddff"
                                            CornerRadius="12"
                                            VerticalAlignment="Center"
                                            Margin="0,0,16,0"
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Mode, Converter={StaticResource ModeToIconConverter}}"
                                                   FontSize="24"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   UseLayoutRounding="True"
                                                   SnapsToDevicePixels="True"
                                                   TextOptions.TextFormattingMode="Display"
                                                   TextOptions.TextRenderingMode="ClearType"
                                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>
                                    
                                    <!-- Основная информация о проекте -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <!-- Название проекта с фиолетовой точкой и статусом -->
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
                                            <Ellipse Fill="{StaticResource PrimaryColor}"
                                                     Width="8"
                                                     Height="8"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Name}" 
                                                       FontSize="18"
                                                       FontWeight="700"
                                                       Foreground="{StaticResource TextPrimary}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,12,0"/>
                                            
                                            <!-- Статус проекта (сразу после названия) -->
                                            <Border Padding="10,6"
                                                    CornerRadius="9999"
                                                    VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <!-- Красный - Не заполнен -->
                                                        <Setter Property="Background" Value="#FEF2F2"/>
                                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                                        <Style.Triggers>
                                                            <!-- Жёлтый - В работе (Ready) -->
                                                            <DataTrigger Binding="{Binding Status}" Value="Ready">
                                                                <Setter Property="Background" Value="#FFFBEB"/>
                                                            </DataTrigger>
                                                            <!-- Зелёный - Готов к печати (SuccessfullyCompleted) -->
                                                            <DataTrigger Binding="{Binding Status}" Value="SuccessfullyCompleted">
                                                                <Setter Property="Background" Value="#D1FAE5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <StackPanel Orientation="Horizontal">
                                                    <!-- Иконка статуса -->
                                                    <TextBlock FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <!-- Красный - Не заполнен -->
                                                                <Setter Property="Text" Value="⚠️"/>
                                                                <Setter Property="Foreground" Value="#DC2626"/>
                                                                <Style.Triggers>
                                                                    <!-- Жёлтый - В работе -->
                                                                    <DataTrigger Binding="{Binding Status}" Value="Ready">
                                                                        <Setter Property="Text" Value="✏️"/>
                                                                        <Setter Property="Foreground" Value="#D97706"/>
                                                                    </DataTrigger>
                                                                    <!-- Зелёный - Готов к печати -->
                                                                    <DataTrigger Binding="{Binding Status}" Value="SuccessfullyCompleted">
                                                                        <Setter Property="Text" Value="✓"/>
                                                                        <Setter Property="Foreground" Value="#065F46"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <!-- Текст статуса -->
                                                    <TextBlock FontSize="13" FontWeight="500" VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <!-- Красный - Не заполнен -->
                                                                <Setter Property="Text" Value="Не заполнен"/>
                                                                <Setter Property="Foreground" Value="#DC2626"/>
                                                                <Style.Triggers>
                                                                    <!-- Жёлтый - В работе -->
                                                                    <DataTrigger Binding="{Binding Status}" Value="Ready">
                                                                        <Setter Property="Text" Value="В работе"/>
                                                                        <Setter Property="Foreground" Value="#D97706"/>
                                                                    </DataTrigger>
                                                                    <!-- Зелёный - Готов к печати -->
                                                                    <DataTrigger Binding="{Binding Status}" Value="SuccessfullyCompleted">
                                                                        <Setter Property="Text" Value="Готов к печати"/>
                                                                        <Setter Property="Foreground" Value="#065F46"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                        
                                        <!-- Мета-информация в один ряд (книги, развороты, дата изменения) -->
                                        <StackPanel Orientation="Horizontal">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                                <TextBlock Text="📖" FontSize="16" Margin="0,0,6,0"/>
                                                <TextBlock FontSize="14" Foreground="{StaticResource TextSecondary}">
                                                    <Run Text="Книг: "/>
                                                    <Run Text="{Binding BookCount}" FontWeight="500"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                                <TextBlock Text="📄" FontSize="16" Margin="0,0,6,0"/>
                                                <TextBlock FontSize="14" Foreground="{StaticResource TextSecondary}">
                                                    <Run Text="Разворотов: "/>
                                                    <Run Text="{Binding PageCount}" FontWeight="500"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <!-- Дата изменения -->
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🕐" FontSize="16" Margin="0,0,6,0"/>
                                                <TextBlock FontSize="14" Foreground="{StaticResource TextSecondary}">
                                                    <Run Text="Изменён: "/>
                                                    <Run Text="{Binding LastModified, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                    
                                    <!-- Кнопки действий (справа) -->
                                    <StackPanel Grid.Column="2" 
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Margin="16,0,0,0">
                                        <Button Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Padding="16,8"
                                                Height="Auto"
                                                FontSize="13"
                                                FontWeight="500"
                                                Margin="0,0,8,0"
                                                Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="#eaddff"/>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="12"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#6750a4"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Opacity" Value="0.8"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🔗" FontSize="16" Margin="0,0,6,0"/>
                                                <TextBlock Text="Открыть"/>
                                            </StackPanel>
                                        </Button>
                                        
                                        <Button Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Padding="16,8"
                                                Height="Auto"
                                                FontSize="13"
                                                FontWeight="500"
                                                Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="#FEF2F2"/>
                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="12"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#DC2626"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🗑️" FontSize="16" Margin="0,0,6,0"/>
                                                <TextBlock Text="Удалить"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Индикатор загрузки -->
        <Grid Grid.RowSpan="2" 
              Background="#CC000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White"
                    CornerRadius="20"
                    Padding="48,40"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="360"
                    Effect="{StaticResource Shadow3}">
                <StackPanel>
                    <TextBlock Text="Загрузка..." 
                               FontSize="18"
                               FontWeight="600"
                               Foreground="{StaticResource TextPrimary}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,32"/>
                    <ProgressBar IsIndeterminate="True" 
                                  Width="300" 
                                  Height="6"
                                  Background="#F8FAFC">
                        <ProgressBar.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#7C3AED" Offset="0"/>
                                <GradientStop Color="#8B5CF6" Offset="1"/>
                            </LinearGradientBrush>
                        </ProgressBar.Foreground>
                    </ProgressBar>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
