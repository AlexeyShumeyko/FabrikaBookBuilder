<UserControl x:Class="PhotoBookRenamer.Views.UniqueFoldersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#eceff5" 
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          ClipToBounds="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/> <!-- Верхняя панель -->
            <RowDefinition Height="*"/> <!-- Рабочая область -->
        </Grid.RowDefinitions>
        
        <!-- Верхняя панель (в стиле CombinedModeView) -->
        <Border Grid.Row="0" 
                Background="White"
                BorderBrush="#E7E0EC"
                BorderThickness="0,0,0,1"
                Height="64">
            <Border.Effect>
                <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
            </Border.Effect>
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Название проекта -->
                <StackPanel Grid.Column="0" 
                            Orientation="Vertical" 
                           VerticalAlignment="Center"
                            HorizontalAlignment="Left">
                    <Grid>
                        <!-- TextBlock для отображения (по умолчанию видим) -->
                        <TextBlock x:Name="ProjectNameTextBlock"
                                   Text="{Binding ProjectName, FallbackValue='Новый проект'}" 
                                   FontSize="20"
                                   FontWeight="600"
                                   Foreground="#1C1B1F"
                                   UseLayoutRounding="True"
                                   SnapsToDevicePixels="True"
                                   TextOptions.TextFormattingMode="Display"
                                   TextOptions.TextRenderingMode="ClearType"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   MouseLeftButtonDown="OnProjectNameClick"
                                   Cursor="Hand"
                                   Padding="2,0"/>
                        <!-- TextBox для редактирования (по умолчанию скрыт) -->
                        <TextBox x:Name="ProjectNameTextBox"
                                 FontSize="20"
                                 FontWeight="600"
                                 Foreground="#1C1B1F"
                                 Background="White"
                                 BorderBrush="#6750A4"
                                 BorderThickness="2"
                                 Padding="4,2"
                                 VerticalContentAlignment="Center"
                                 LostFocus="OnProjectNameLostFocus"
                                 KeyDown="OnProjectNameKeyDown"
                                 Visibility="Collapsed"
                                 MinWidth="200"/>
                    </Grid>
                    <TextBlock Text="Режим уникальных папок" 
                               FontSize="14"
                               Foreground="#625B71"
                               Margin="0,4,0,0"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                </StackPanel>
                
                <!-- Кнопка "Загрузить ещё папки" (по центру) -->
                <Button Grid.Column="1"
                        Command="{Binding LoadFoldersCommand}"
                        HorizontalAlignment="Left"
                        Margin="16,0,0,0"
                        VerticalAlignment="Center"
                        Padding="16,8"
                        FontSize="14"
                        FontWeight="500"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                        Visibility="{Binding Books.Count, Converter={StaticResource NullToVisibilityConverter}}"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#F5F3FF"/>
                            <Setter Property="Foreground" Value="#6750A4"/>
                            <Setter Property="BorderBrush" Value="#E7E0EC"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.06" Color="#6750A4"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#EADDFF"/>
                                    <Setter Property="BorderBrush" Value="#6750A4"/>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.1" Color="#6750A4"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📁" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Загрузить ещё папки" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- Кнопки управления -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right">
                    <Button Command="{Binding BackCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FEF7FF"/>
                                <Setter Property="Foreground" Value="#6750A4"/>
                                <Setter Property="BorderBrush" Value="#79747E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E7E0EC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="←" FontSize="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Назад" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding SaveProjectCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FEF7FF"/>
                                <Setter Property="Foreground" Value="#6750A4"/>
                                <Setter Property="BorderBrush" Value="#79747E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E7E0EC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" FontSize="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Сохранить" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,8,0"
                            Cursor="Hand"
                            IsEnabled="{Binding Books.Count, Converter={StaticResource CountToBooleanConverter}}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#6750A4"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5A4B8F"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <TranslateTransform Y="-1"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.15" Color="#000000"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📤" FontSize="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Экспорт" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenHelpCommand}"
                            Padding="8,8"
                            Height="36"
                            FontSize="14"
                            FontWeight="500"
                            Margin="0,0,0,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#625B71"/>
                                <Setter Property="BorderBrush" Value="#79747E"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F3FF"/>
                                        <Setter Property="Foreground" Value="#6750A4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="?" FontSize="18" FontWeight="600" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Помощь" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Рабочая область -->
        <Grid Grid.Row="1" Margin="24">
        <!-- Error Message -->
            <Border Background="#7F1D1D" 
                    Padding="24" 
                    Margin="0,24,0,0"
                CornerRadius="16"
                    BorderBrush="#EF4444"
                BorderThickness="2"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                Panel.ZIndex="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#EF4444"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="❌ Ошибка" 
                               Foreground="#EF4444" 
                               FontSize="18"
                               FontWeight="600"
                               Margin="0,0,0,12"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"/>
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="#FEE2E2" 
                               FontSize="14"
                               TextWrapping="Wrap"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"/>
            </StackPanel>
        </Border>
        
        <!-- Empty State -->
            <Border Visibility="{Binding Books.Count, Converter={StaticResource InverseNullToVisibilityConverter}}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Panel.ZIndex="1"
                IsHitTestVisible="True">
            <StackPanel>
                    <Border Background="#EADDFF" 
                        CornerRadius="32" 
                        Width="140" Height="140"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,32">
                    <Border.Effect>
                            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.12" Color="#6750A4"/>
                    </Border.Effect>
                    <TextBlock Text="📁" FontSize="84" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               Foreground="#6750A4"/>
                </Border>
                <TextBlock Text="Начните работу с загрузки папок" 
                               FontSize="24"
                               FontWeight="600"
                               Foreground="#1C1B1F"
                           HorizontalAlignment="Center"
                               Margin="0,0,0,20"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"/>
                <TextBlock Text="Выберите папки с JPG/JPEG файлами.&#x0a;Каждая папка будет преобразована в отдельную книгу.&#x0a;Все папки должны содержать одинаковое количество файлов." 
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                               FontSize="14"
                               Foreground="#625B71"
                           LineHeight="28"
                           Margin="0,0,0,40"
                               MaxWidth="450"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"/>
                    <Button Command="{Binding LoadFoldersCommand}"
                        HorizontalAlignment="Center"
                        Padding="32,16"
                        FontSize="16"
                            FontWeight="500"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#6750A4"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="#6750A4"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="12"
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.08" Color="#6750A4"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5A4B8F"/>
                                        <Setter Property="BorderBrush" Value="#5A4B8F"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <TranslateTransform Y="-1"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.12" Color="#6750A4"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📁" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Загрузить папки" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
            </StackPanel>
        </Border>
        
        <!-- Books List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      Visibility="{Binding Books.Count, Converter={StaticResource NullToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding Books}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                            <Border BorderBrush="#E7E0EC" 
                                BorderThickness="1" 
                                Margin="0,16" 
                                    Padding="24" 
                                    Background="White"
                                CornerRadius="20"
                                ClipToBounds="False">
                            <Border.Effect>
                                    <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.12" Color="#000000" RenderingBias="Quality"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Header with Icon and Name -->
                                    <Grid Grid.Row="0" Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Folder Icon -->
                                    <Border Grid.Column="0" 
                                                Background="#EADDFF" 
                                            CornerRadius="12" 
                                            Padding="14" 
                                            Margin="0,0,16,0" 
                                            VerticalAlignment="Center">
                                        <Border.Effect>
                                                <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.08" Color="#6750A4"/>
                                        </Border.Effect>
                                        <TextBlock Text="📁" FontSize="32"/>
                                    </Border>
                                    
                                    <!-- Book Name -->
                                    <TextBlock Grid.Column="1" 
                                               Text="{Binding Name}" 
                                                   FontSize="18"
                                                   FontWeight="600"
                                                   Foreground="#1C1B1F"
                                                   VerticalAlignment="Center"
                                                   UseLayoutRounding="True"
                                                   SnapsToDevicePixels="True"
                                                   TextOptions.TextFormattingMode="Display"
                                                   TextOptions.TextRenderingMode="ClearType"/>
                                    
                                    <!-- Book Index and Delete -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" 
                                                Margin="20,0,0,0"
                                                VerticalAlignment="Center">
                                            <Border Background="#6750A4" 
                                                CornerRadius="8" 
                                                    Padding="12,8" 
                                                Margin="0,0,12,0"
                                                    MinWidth="100">
                                            <TextBlock Text="{Binding BookIndex, StringFormat='Книга {0}'}" 
                                                           FontSize="13"
                                                           FontWeight="500"
                                                           Foreground="White"
                                                       HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           UseLayoutRounding="True"
                                                           SnapsToDevicePixels="True"
                                                           TextOptions.TextFormattingMode="Display"
                                                           TextOptions.TextRenderingMode="ClearType"/>
                                        </Border>
                                            <Button Command="{Binding DataContext.DeleteBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                    Padding="12,8"
                                                MinWidth="90"
                                                FontSize="13"
                                                    FontWeight="500"
                                                    VerticalAlignment="Center"
                                                    Cursor="Hand">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Background" Value="#BA1A1A"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            CornerRadius="8"
                                                                            Padding="{TemplateBinding Padding}">
                                                                        <Border.Effect>
                                                                            <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
                                                                        </Border.Effect>
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#9A1A1A"/>
                                                                <Setter Property="RenderTransform">
                                                                    <Setter.Value>
                                                                        <TranslateTransform Y="-1"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🗑️" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="Удалить" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                    </StackPanel>
                                </Grid>
                                
                                <!-- Cover and Pages in one row - 5 items per row -->
                                <ItemsControl Grid.Row="1" 
                                              x:Name="AllSlotsItemsControl" 
                                              Tag="{Binding}" 
                                              DataContext="{Binding}">
                                    <ItemsControl.ItemsSource>
                                        <Binding Path="AllSlots"/>
                                    </ItemsControl.ItemsSource>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="5"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#E7E0EC" 
                                                    BorderThickness="1" 
                                                    CornerRadius="12" 
                                                    Padding="8" 
                                                    Margin="6"
                                                    Background="White"
                                                    DataContext="{Binding}"
                                                    Height="220"
                                                    Loaded="OnCellLoaded">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.08" Color="#000000"/>
                                                </Border.Effect>
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <!-- Заголовок: Обложка или Стр. X -->
                                                    <TextBlock Grid.Row="0"
                                                               FontWeight="Semibold"
                                                               FontSize="12"
                                                               Foreground="#1C1B1F"
                                                               HorizontalAlignment="Center"
                                                               Margin="0,0,0,4"
                                                               UseLayoutRounding="True"
                                                               SnapsToDevicePixels="True"
                                                               TextOptions.TextFormattingMode="Display"
                                                               TextOptions.TextRenderingMode="ClearType">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="{Binding StringFormat='Разв. {0}'}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding}" Value="0">
                                                                        <Setter Property="Text" Value="Обложка"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    
                                                    <!-- Фото: обложка или страница - Grid растягивается по ширине ячейки -->
                                                    <Grid Grid.Row="1"
                                                          Background="Transparent"
                                                          ClipToBounds="True"
                                                          HorizontalAlignment="Stretch"
                                                          VerticalAlignment="Stretch"
                                                          Loaded="OnPhotoGridLoaded">
                                                        <Image Stretch="Fill"
                                                               HorizontalAlignment="Stretch"
                                                               VerticalAlignment="Stretch"
                                                               Cursor="Hand"
                                                               ToolTip="Двойной клик для открытия"
                                                               Loaded="OnImageLoaded"
                                                               SizeChanged="OnImageSizeChanged"
                                                               LayoutUpdated="OnImageLayoutUpdated">
                                                            <Image.Source>
                                                                <MultiBinding Converter="{StaticResource CoverOrPageByIndexConverter}">
                                                                    <Binding Path="DataContext.Cover" RelativeSource="{RelativeSource AncestorType=ItemsControl, AncestorLevel=1}"/>
                                                                    <Binding Path="DataContext.Pages" RelativeSource="{RelativeSource AncestorType=ItemsControl, AncestorLevel=1}"/>
                                                                    <Binding/>
                                                                </MultiBinding>
                                                            </Image.Source>
                                                            <Image.Style>
                                                                <Style TargetType="Image">
                                                                    <Setter Property="Opacity" Value="1"/>
                                                                    <EventSetter Event="MouseLeftButtonDown" Handler="OnPageImageDoubleClick"/>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Opacity" Value="0.8"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Image.Style>
                                                            <Image.Tag>
                                                                <Binding/>
                                                            </Image.Tag>
                                                        </Image>
                                                        
                                                        <!-- Кнопка "Изменить" для обложки - накладывается на фото -->
                                                        <Button Content="Изменить" 
                                                                Command="{Binding DataContext.SelectCoverCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=1}}"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Bottom"
                                                                Margin="0,0,0,8"
                                                                Padding="6,4"
                                                                FontSize="10"
                                                                FontWeight="500"
                                                                Visibility="{Binding Converter={StaticResource IsZeroToVisibilityConverter}}"
                                                                Cursor="Hand"
                                                                Panel.ZIndex="10">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="#FEF7FF"/>
                                                                    <Setter Property="Foreground" Value="#6750A4"/>
                                                                    <Setter Property="BorderBrush" Value="#79747E"/>
                                                                    <Setter Property="BorderThickness" Value="1"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                                        CornerRadius="8"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#E7E0EC"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                        
                                                        <!-- Имя файла для страниц - накладывается на фото -->
                                                        <TextBlock HorizontalAlignment="Center"
                                                                   VerticalAlignment="Top"
                                                                   Margin="0,4,0,0"
                                                                   FontSize="9"
                                                                   Foreground="#625B71"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   Visibility="{Binding Converter={StaticResource IsNotZeroToVisibilityConverter}}"
                                                                   UseLayoutRounding="True"
                                                                   SnapsToDevicePixels="True"
                                                                   TextOptions.TextFormattingMode="Display"
                                                                   TextOptions.TextRenderingMode="ClearType"
                                                                   Panel.ZIndex="10">
                                                            <TextBlock.Text>
                                                                <MultiBinding Converter="{StaticResource PageFileNameConverter}">
                                                                    <Binding Path="DataContext.Pages" RelativeSource="{RelativeSource AncestorType=ItemsControl, AncestorLevel=1}"/>
                                                                    <Binding/>
                                                                </MultiBinding>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                        
                                                        <!-- Кнопки управления для страниц - накладываются на фото -->
                                                        <StackPanel Orientation="Horizontal" 
                                                                    HorizontalAlignment="Center"
                                                                    VerticalAlignment="Bottom"
                                                                    Margin="0,0,0,8"
                                                                    Visibility="{Binding Converter={StaticResource IsNotZeroToVisibilityConverter}}"
                                                                    Panel.ZIndex="10">
                                                        <Button Content="◄" 
                                                                Width="28"
                                                                Height="28"
                                                                FontSize="12"
                                                                FontWeight="600"
                                                                ToolTip="Переместить влево (раньше)"
                                                                Click="OnMovePageLeftClick"
                                                                Tag="{Binding}"
                                                                Margin="0,0,3,0"
                                                                Cursor="Hand">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="#6750A4"/>
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        CornerRadius="6"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#5A4B8F"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                        <Button Content="►" 
                                                                Width="28"
                                                                Height="28"
                                                                FontSize="12"
                                                                FontWeight="600"
                                                                ToolTip="Переместить вправо (позже)"
                                                                Click="OnMovePageRightClick"
                                                                Tag="{Binding}"
                                                                Margin="0,0,3,0"
                                                                Cursor="Hand">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="#6750A4"/>
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        CornerRadius="6"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#5A4B8F"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                        <Button Content="№" 
                                                                Width="28"
                                                                Height="28"
                                                                FontSize="12"
                                                                FontWeight="600"
                                                                ToolTip="Назначить номер разворота"
                                                                Click="OnAssignPageNumberClick"
                                                                Tag="{Binding}"
                                                                Cursor="Hand">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="#6750A4"/>
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}"
                                                                                        CornerRadius="6"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#5A4B8F"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                        </StackPanel>
                                                    </Grid>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        </Grid>
        
        <!-- Loading Indicator -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Background="#CC000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White"
                    CornerRadius="20"
                    Padding="48,40" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    MinWidth="360"
                    ClipToBounds="False">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.25" Color="#000000" RenderingBias="Quality"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Загрузка..." 
                               FontSize="20"
                               FontWeight="600"
                               Foreground="#1C1B1F"
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,32"
                               UseLayoutRounding="True"
                               SnapsToDevicePixels="True"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="ClearType"/>
                    <ProgressBar IsIndeterminate="True" 
                                 Height="6" 
                                 Width="300"
                                 Background="#E7E0EC">
                        <ProgressBar.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#6750A4" Offset="0"/>
                                <GradientStop Color="#8651F3" Offset="1"/>
                            </LinearGradientBrush>
                        </ProgressBar.Foreground>
                    </ProgressBar>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
