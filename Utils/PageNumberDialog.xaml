<Window x:Class="PhotoBookRenamer.Utils.PageNumberDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Назначить разворот" 
        Height="240" 
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border Background="{StaticResource BackgroundCard}" 
            CornerRadius="20"
            BorderThickness="0"
            ClipToBounds="False">
        <Border.Effect>
            <DropShadowEffect BlurRadius="48" ShadowDepth="0" Opacity="0.3" Color="#000000" RenderingBias="Quality"/>
        </Border.Effect>
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0"
                       Text="Введите номер разворота, на который нужно переместить это фото:"
                       TextWrapping="Wrap"
                       Style="{StaticResource FluentBodyStyle}"
                       Margin="0,0,0,20"/>
            
            <TextBlock Grid.Row="1"
                       Text="Номер разворота:"
                       Style="{StaticResource FluentBodyMedium}"
                       Margin="0,0,0,10"/>
            
            <TextBox Grid.Row="2"
                     x:Name="PageNumberTextBox"
                     Style="{StaticResource FluentTextBoxStyle}"
                     FontSize="15"
                     Margin="0,0,0,28"
                     KeyDown="PageNumberTextBox_KeyDown"/>
            
            <StackPanel Grid.Row="4" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Отмена" 
                        Click="CancelButton_Click"
                        Style="{StaticResource FluentOutlinedButton}"
                        Padding="20,10"
                        Margin="0,0,12,0"
                        MinWidth="100"/>
                <Button Content="Переместить" 
                        Click="OkButton_Click"
                        Style="{StaticResource FluentFilledButton}"
                        Padding="20,10"
                        MinWidth="120"
                        x:Name="OkButton"
                        IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>






